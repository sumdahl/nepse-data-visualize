import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server Actions are stable in Next.js 16, no special config needed
  // Body size limit is handled by Next.js defaults
};

export default nextConfig;
